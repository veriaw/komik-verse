<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.KomikVerse" parent="Theme.Material3.DayNight">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="colorPrimary">@color/lightPurple</item>
        <item name="colorOnPrimary">@color/black</item>

        <item name="colorSecondary">@color/deepPurple500</item>
        <item name="colorOnSecondary">@color/white</item>
    </style>

    <style name="Theme.KomikVerse" parent="Base.Theme.KomikVerse" />
    
    <style name="Theme.App.SplashScreen" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/lightPurple</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/splash</item>
        <item name="postSplashScreenTheme">@style/Base.Theme.KomikVerse</item>
    </style>
</resources>